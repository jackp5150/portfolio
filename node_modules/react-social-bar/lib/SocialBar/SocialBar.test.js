'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactDom = require('react-dom');

var _reactDom2 = _interopRequireDefault(_reactDom);

var _enzyme = require('enzyme');

var _ = require('./');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('<SocialBar />', function () {

  var wrapper = (0, _enzyme.mount)(_react2.default.createElement(_2.default, null));
  var shallowWrapper = (0, _enzyme.shallow)(_react2.default.createElement(_2.default, null));

  it('should render without crashing', function () {
    var div = document.createElement('div');
    _reactDom2.default.render(_react2.default.createElement(_2.default, null), div);
  });

  // COMPONENT

  it('should render 4 <SocialBox />', function () {
    var selector = 'SocialBox';
    expect(shallowWrapper.find(selector).length).toEqual(4);
  });

  it('should have a _handleTwitter method', function () {
    expect(wrapper.instance()._handleTwitter).not.toBeNull();
    expect(wrapper.instance()._handleTwitter).toBeDefined();
  });

  it('should have a _handlePinterest method', function () {
    expect(wrapper.instance()._handlePinterest).not.toBeNull();
    expect(wrapper.instance()._handlePinterest).toBeDefined();
  });

  it('should have a _handleFacebook method', function () {
    expect(wrapper.instance()._handleFacebook).not.toBeNull();
    expect(wrapper.instance()._handleFacebook).toBeDefined();
  });

  it('should have a _handleLinkedIn method', function () {
    expect(wrapper.instance()._handleLinkedIn).not.toBeNull();
    expect(wrapper.instance()._handleLinkedIn).toBeDefined();
  });

  // STATE
});